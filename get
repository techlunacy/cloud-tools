

cfm = AWS::CloudFormation.new
stack = cfm.stacks['sdstaging']
filename = "test.json"
File.mv(filename, "#{filename}.#{Time.now.to_i.to_s}") if File.exists?(filename)
File.open(filename, mode= "w+"){|file| file.write(stack.template) }